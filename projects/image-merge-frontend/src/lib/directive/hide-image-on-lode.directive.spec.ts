import { HideImageOnLodeDirective } from './hide-image-on-lode.directive';

describe('HideImageOnLodeDirective', () => {
  it('should create an instance', () => {
    const directive = new HideImageOnLodeDirective();
    expect(directive).toBeTruthy();
  });
});
